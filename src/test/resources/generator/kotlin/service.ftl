package ${project.packageName}.service

import ${project.packageName}.service.base.Base${entity.name}Service

import org.springframework.stereotype.Service


@Service
class ${entity.name}Service : Base${entity.name}Service()

