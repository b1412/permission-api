package ${project.packageName}.dao

import ${project.packageName}.dao.base.Base${entity.name}Dao
import org.springframework.stereotype.Repository

@Repository
interface ${entity.name}Dao : Base${entity.name}Dao
